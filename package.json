{
  "name": "autocomplete-sass",
  "version": "0.0.0",
  "description": "CSS property name and value autocompletions for SASS",
  "main": "./lib/main",
  "license": "MIT",
  "repository": "https://github.com/saschagehlich/autocomplete-sass",
  "engines": {
    "atom": ">=0.174.0 <2.0.0"
  },
  "providedServices": {
    "autocomplete.provider": {
      "versions": {
        "2.0.0": "getProvider"
      }
    }
  },
  "devDependencies": {
    "bluebird": "^2.9.25",
    "request": "^2.53.0"
  }
}
